# Screenshot: Profile Screen

Drop a screenshot of the **Profile screen** here. Rename to `screenshot.png`.

**What to capture:** Header with back button + "Profile" title, circular avatar (User icon on dark background), user name and email, plan card (Free or Pro), usage bar, Upgrade card (or "Pro Plan Active" if subscribed), Settings row, Restore Purchases row, and Sign Out button.
