# Screenshot: Replace Object — Draw Region

Drop a screenshot of the **Replace Object select screen** here. Rename to `screenshot.png`.

**What to capture:** Full-screen photo with a dashed selection rectangle drawn on it (the user drags to select the object area), back button top-left, reset button, and "Continue" button at the bottom.
